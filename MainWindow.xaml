<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ww="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Background="Transparent"
        Title="MainWindow" Height="600" Icon="assets/menu.png" Width="800">
    <Border Name="host_" BorderThickness="2" BorderBrush="White" Background="#242424" CornerRadius="8">
        <Grid x:Name="host__">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button Name="Minimize" Click="Minimize_Click" Margin="0,5,5,0" Template="{StaticResource TestStyleBT}" MinHeight="30" Background="White" MinWidth="30">
                    <Image Source="assets/minimize.png" Width="12"/>
                </Button>
                <Button Name="Exit" Click="Exit_Click" Margin="0,5,5,0" Template="{StaticResource TestStyleBT}" MinHeight="30" MinWidth="30">
                    <Image Source="assets/close.png" Width="12"/>
                </Button>
            </StackPanel>
            <Label Name="TitleAnimated" VerticalAlignment="Top" FontSize="17" FontFamily="Consolas" FontWeight="Bold" HorizontalAlignment="Left" Content="ExampleAppWPF" Foreground="White"/>
            <Grid Margin="0,40,0,0 " Name="Main">
                <ww:WebView2 Name="TestEditor" ZoomFactor="0.80"/>
            </Grid>
        </Grid>
    </Border>
</Window>
